# macOS specific
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon\r

# Thumbnails
._*

# Temporary files
.TemporaryItems
.Trashes
.Spotlight-V100
.VolumeIcon.icns
.fseventsd

# Node.js dependencies
node_modules/

# Build output
dist/
out/
build/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# dotenv environment variable files
.env
.env.local
.env.test
.env.production

# Optional npm cache directory
.npm/

# macOS temporary and metadata files
*.swp
*~
._*

# ESLint cache
.eslintcache

# Prettier cache
.prettiercache

# Coverage directory used by testing tools like Jest
coverage/

# IDE and Editor specific
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Mac specific files
.DS_Store
.Trashes
.Spotlight-V100

# Lock files
# Libraries: If you are building a library (as opposed to an application), 
#  some teams prefer not to commit package-lock.json because 
#  it may lock sub-dependencies that the end-users of your library may not necessarily want locked.
#  However, even for libraries, the lock file can still help maintain consistency during development.

package-lock.json
yarn.lock
pnpm-lock.yaml

# Compiled JavaScript from TypeScript
*.js
*.d.ts

# Git directory
.git/

# System-specific files
Thumbs.db
Desktop.ini